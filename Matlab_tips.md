Matlab Tipes  
===========

배열 복사  
------------
```matlab
visited_map = zeros(size(map));
visiited_map = map(:,:);
```

큐
------------
```matlab
queue=parallel.pool.PollableDataQueue

queue.send(current_point)
queue.poll()
```

dd
